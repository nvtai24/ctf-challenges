apiVersion: apps/v1
kind: Deployment
metadata:
  name: RaceCondition
spec:
  replicas: 1
  selector:
    matchLabels:
      app: RaceCondition
  template:
    metadata:
      labels:
        app: RaceCondition
    spec:
      containers:
        - name: webapp
          image: ""
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: "200m"
              memory: "256Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"
