apiVersion: apps/v1
kind: Deployment
metadata:
  name: Ret2Libc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: Ret2Libc
  template:
    metadata:
      labels:
        app: Ret2Libc
    spec:
      containers:
        - name: webapp
          image: ""
          ports:
            - containerPort: 1337
          resources:
            limits:
              cpu: "200m"
              memory: "128Mi"
            requests:
              cpu: "100m"
              memory: "64Mi"
