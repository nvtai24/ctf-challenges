apiVersion: apps/v1
kind: Deployment
metadata:
  name: SQLiLogin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: SQLiLogin
  template:
    metadata:
      labels:
        app: SQLiLogin
    spec:
      containers:
        - name: webapp
          image: ""
          ports:
            - containerPort: 5000
          resources:
            limits:
              cpu: "200m"
              memory: "256Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"
