apiVersion: apps/v1
kind: Deployment
metadata:
  name: IntegerOverflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: IntegerOverflow
  template:
    metadata:
      labels:
        app: IntegerOverflow
    spec:
      containers:
        - name: webapp
          image: ""
          ports:
            - containerPort: 1337
          resources:
            limits:
              cpu: "200m"
              memory: "128Mi"
            requests:
              cpu: "100m"
              memory: "64Mi"
